name: Demo Deploy
on:
  push:
    tags: ['v*.*.*']
  workflow_dispatch:

jobs:
  build-and-deploy:
    name: Build & Deploy Python demo
    runs-on: ubuntu-latest
    steps:
      # Python demo 依赖共享流程 JSON，需 checkout jeeflow-java
      - name: Checkout jeeflow-java
        uses: actions/checkout@v4
        with:
          repository: mldong/jeeflow-java
          path: jeeflow-java
      - name: Checkout jeeflow-python
        uses: actions/checkout@v4
        with:
          path: jeeflow-python
      - name: Build Docker image
        run: |
          # Dockerfile.demo 期望构建上下文包含 jeeflow-java/ 和 python 源码
          mkdir -p /tmp/demo-build
          cp -r jeeflow-python /tmp/demo-build/
          cp -r jeeflow-java /tmp/demo-build/
          docker build -f /tmp/demo-build/jeeflow-python/Dockerfile.demo -t jeeflow-python-demo:latest /tmp/demo-build/
      - name: Export image
        run: docker save jeeflow-python-demo:latest -o jeeflow-python-demo.tar
      - name: Upload to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: root
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          source: jeeflow-python-demo.tar
          target: /java_projects/jeeflow/demo/
      - name: Load image and restart
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: root
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          script: |
            cd /java_projects/jeeflow/demo
            docker load -i jeeflow-python-demo.tar
            docker-compose up -d jeeflow-python-demo
            rm -f jeeflow-python-demo.tar
            echo "✅ Python demo deployed"
